/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'classic-actor-79ca20f4f3.media.strapiapp.com',
      'localhost',
      'res.cloudinary.com',
    ],
  },
};

module.exports = nextConfig;
